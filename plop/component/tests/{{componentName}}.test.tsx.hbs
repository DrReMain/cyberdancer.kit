import { renderHook } from "@cyber-ui/test-utils";
import * as React from "react";
import { use{{capitalize componentName}} } from "../src";

describe("use{{capitalize componentName}}", () => {
  test("it works", () => {
    expect(true).toBeTruthy();
  });
});